@model Nexos.Models.Sistema

@{
    ViewData["Title"] = "Editar Sistema";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container pt-50 pb-50">
    <div class="row">
        <div class="col-lg-8 offset-lg-2">
            <h1 class="section-title mb-40">Editar Sistema</h1>
            <div class="card bg-dark text-white p-4">
                <form asp-action="Edit">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                    <input type="hidden" asp-for="ID_Sistema" />
                    <div class="form-group mb-3">
                        <label asp-for="Nome_Sistema" class="control-label"></label>
                        <input asp-for="Nome_Sistema" class="form-control bg-secondary text-white border-0" />
                        <span asp-validation-for="Nome_Sistema" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <input type="submit" value="Salvar" class="btn hero-btn" />
                        <a asp-action="Index" class="btn btn-secondary ml-2">Voltar para a Lista</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
"""
